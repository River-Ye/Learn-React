
const initinalState = {
  loading: false,
};

const common = (state = initinalState, action) => {
  // TODO
  /* 
  switch (action.type) {
    case 'CHANGE_LOADING':
      return {
        ...state,
        loading: action.value,
      };

    default:
      return state;
  } 
  // */
};

export default common;
